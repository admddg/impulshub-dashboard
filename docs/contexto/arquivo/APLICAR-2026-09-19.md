# Para colar no SQL Editor do Clients_Base — 19/09/2026

## Situação

**Parte 2 (ledger) — ✅ aplicada.** As 11 migrations do ledger batem com os
arquivos do repositório.

**Parte 1 (trigger de `stage_version`) — ❌ NÃO aplicada.** Conferido no banco:

```
crm.bump_stage_version existe?                0
opportunities_bump_stage_version existe?      0
linha 20260925000002 no ledger?               1
```

O ledger registra a migration como aplicada, mas os objetos não existem. Esse é
o pior dos estados: um `supabase db push` futuro **pula** a migration por já
estar registrada, e o trigger nunca é criado.

Rodar o bloco abaixo resolve — ele cria os objetos e a linha do ledger passa a
ser verdade. Não é preciso mexer no ledger de novo.

---

## O que a Parte 1 faz

O parser promove Lead → Atendimento com um UPDATE direto e nunca tocou
`stage_version`. Medido em produção: **259 de 259** oportunidades em atendimento
na versão 0.

Efeito prático: a tela carrega o card em Lead na versão 0, o parser move para
Atendimento (ainda versão 0), e a ação do usuário passa pela checagem de
conflito como se nada tivesse acontecido. A trava otimista existe para esse caso
exato e está cega.

A correção fica no banco e não no parser, para valer também para código futuro.
As RPCs já fazem o `+1` explícito e continuam corretas: o trigger grava o mesmo
valor.

---

## O bloco

Pode rodar quantas vezes quiser — é idempotente. E **não passa em silêncio**: se
o trigger não existir ao final, o bloco inteiro é abortado com erro em vez de
dar "sucesso".

```sql
begin;

set local lock_timeout = '5s';

create or replace function crm.bump_stage_version()
returns trigger
language plpgsql
security definer
set search_path = ''
as $fn$
begin
  if new.current_stage_id is distinct from old.current_stage_id then
    new.stage_version := old.stage_version + 1;
  end if;
  return new;
end;
$fn$;

revoke all on function crm.bump_stage_version() from public, anon, authenticated, service_role;

drop trigger if exists opportunities_bump_stage_version on crm.opportunities;

-- BEFORE de propósito: a ponte de conversões (AFTER UPDATE) precisa enxergar a
-- versão já incrementada no payload que envia.
create trigger opportunities_bump_stage_version
before update of current_stage_id on crm.opportunities
for each row execute function crm.bump_stage_version();

comment on function crm.bump_stage_version() is
  'Garante stage_version = anterior + 1 em qualquer troca de etapa, inclusive movimentos automaticos do parser.';

-- Trava de segurança: se o trigger não estiver de pé ao chegar aqui, aborta
-- tudo em vez de reportar sucesso. Foi exatamente esse silêncio que deixou o
-- ledger mentindo na primeira tentativa.
do $check$
begin
  if not exists (
    select 1 from pg_trigger
     where tgrelid = 'crm.opportunities'::regclass
       and tgname = 'opportunities_bump_stage_version'
  ) then
    raise exception 'FALHOU: opportunities_bump_stage_version nao foi criado';
  end if;
end
$check$;

commit;
```

---

## Conferir depois de rodar

```sql
select
  (select count(*) from pg_proc p join pg_namespace n on n.oid = p.pronamespace
    where n.nspname = 'crm' and p.proname = 'bump_stage_version')   as funcao,
  (select count(*) from pg_trigger
    where tgrelid = 'crm.opportunities'::regclass
      and tgname = 'opportunities_bump_stage_version')              as trigger;
```

Tem que voltar `1` e `1`.

O teste de verdade é mover um card pelo painel duas vezes: a versão sai de N
para N+1 e depois N+2. Antes, movimento automático do parser não mexia no
número.

---

## Anexo — Parte 2, já aplicada (só registro)

Quatro migrations estavam registradas com o carimbo de quando foram aplicadas,
não com o nome do arquivo no repositório, e `client_users_attendant_role` não
tinha arquivo nenhum — foi escrito a partir do texto exato guardado no próprio
ledger.

| Ledger antes | Passou a ser | Arquivo no repo |
|---|---|---|
| `20260918203014` | `20260923000004` | `20260923000004_client_users_attendant_role.sql` |
| `20260918203606` | `20260924000000` | `20260924000000_crm_event_bridge.sql` |
| `20260919114524` | `20260925000000` | `20260925000000_crm_fix_owners_can_write.sql` |
| `20260919114551` | `20260925000001` | `20260925000001_crm_board_counts_filtered.sql` |

```sql
-- JÁ APLICADO EM 19/09. Não rodar de novo.
update supabase_migrations.schema_migrations
   set version = '20260923000004' where version = '20260918203014';
update supabase_migrations.schema_migrations
   set version = '20260924000000' where version = '20260918203606';
update supabase_migrations.schema_migrations
   set version = '20260925000000' where version = '20260919114524';
update supabase_migrations.schema_migrations
   set version = '20260925000001' where version = '20260919114551';
insert into supabase_migrations.schema_migrations (version, name)
values ('20260925000002', 'crm_stage_version_guard')
on conflict (version) do nothing;
```
