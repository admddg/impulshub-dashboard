import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Tracking · Agência' }
export default function L({ children }: { children: React.ReactNode }) { return <>{children}</> }
